@using MoonCycle.Utils
@using MoonCycle.Enums

<div class="moonphase-title">
	<p>@MoonphaseUtils.GetMoonPhaseString(MoonPhase)</p>
	<MoonPhaseIcon MoonPhase="MoonPhase" />
</div>

@code {
	[Parameter]
	public MoonPhase MoonPhase { get; set; }
}
